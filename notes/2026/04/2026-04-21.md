# 2026-04-18 | HackQuest Day 27

**Time:** 20min

---

## 코드 

###

### SendETH.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract SendETH {
    function sendETH(address payable _to, uint256 _amount) public payable {
        require(msg.value >= _amount, "Insufficient ETH sent");
        _to.transfer(_amount);
    }
}

### Metamask 연결
- 메타마스크는 정상적으로 연결하고
- 테스트넷 네트워크 환경도 설정하고 전환



```


## 느낀 점 / 다음에 볼 것
